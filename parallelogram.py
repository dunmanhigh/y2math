#find the area of a parellelogram using the formula of area of parellelogram

formula=  base = float(input('Length of base: '))
          height = float(input('Measurement of height: '))
          area = base * height
          print("Area is: ", area)

base = 9
height = 5
