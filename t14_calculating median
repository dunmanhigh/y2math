# Find median of n numbers
X = [4, 1, 5, 6, 2, 5]

X.sort()
print(X)

mid = len(X) // 2
if len(X) % 2 == 0: # even n, take average of middle 2 values
  median = (X[mid-1] + X[mid]) / 2
else: # odd n
  median = X[mid]
print(median)
